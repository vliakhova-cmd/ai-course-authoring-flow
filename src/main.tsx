import React from 'react';
import ReactDOM from 'react-dom/client';
import { ContentLibraryLauncher } from './ContentLibraryLauncher';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ContentLibraryLauncher />
  </React.StrictMode>,
);
